package chess;
import chess.board.*;
import com.chess.gui.*;
public class Chess {

	public static void main(String[] args) {
		
		Table table = new Table();
		//board.printBoard(board.getSquares());
		//board.applyMove(board.getSquare(1, 3).getPiece().getLegalMoves(board, board.getSquare(1, 3)).get(1));
		//board.printBoard(board.getSquares());
	}

}
